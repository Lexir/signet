-- signet :: сброс курсоров синка после дедупа (V3)
--
-- V3 пересоздал таблицу тел зеркала (mail_messages стала пустой), но оставил в mail_folders
-- курсоры инкрементального синка (last_synced_uid / uid_validity). Из-за этого синк считал
-- папки уже вычитанными и тянул только НОВЫЕ письма (uid > курсора), не перезаливая
-- существующие — зеркало оставалось пустым. Сбрасываем курсоры, чтобы папки синкнулись заново.

UPDATE mail_folders SET last_synced_uid = 0, uid_validity = NULL, synced_at = NULL;
