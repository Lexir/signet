# Reverse proxy перед Signet. Всё внешнее (80/443) терминируется здесь,
# приложение слушает только внутри сети compose (app:8080).
#
# Заголовки X-Forwarded-* обязательны: по ним приложение (профиль prod,
# TRUST_FORWARDED_HEADERS=true) видит реальный IP клиента для rate limit
# и защиты от перебора пароля.

server {
    listen 80;
    server_name _;

    # Когда включите TLS-блок ниже — раскомментируйте редирект,
    # чтобы по http наружу ничего не отвечало, кроме перенаправления.
    # return 301 https://$host$request_uri;

    # Вложения писем скачиваются через приложение — не режем их на прокси.
    client_max_body_size 25m;

    location / {
        proxy_pass http://app:8080;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;    # полный синк ящика может идти десятки секунд
    }
}

# --- HTTPS ---
# 1) Положите сертификат и ключ в ./nginx/certs (fullchain.pem, privkey.pem) —
#    например, от certbot (certonly --standalone) или из панели провайдера.
# 2) Раскомментируйте volume ./nginx/certs в docker-compose.yml и блок ниже.
# 3) Уберите COOKIE_SECURE=false из .env (по умолчанию cookie требует HTTPS).
#
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name _;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     client_max_body_size 25m;
#
#     location / {
#         proxy_pass http://app:8080;
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 60s;
#     }
# }
